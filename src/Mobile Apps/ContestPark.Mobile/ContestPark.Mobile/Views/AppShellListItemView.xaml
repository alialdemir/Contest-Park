<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ContestPark.Mobile.Views.AppShellListItemView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ContentView.Content>
        <Grid Margin="16,16,16,0" ColumnSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Image
                Grid.Row="0"
                Grid.Column="0"
                HeightRequest="32"
                HorizontalOptions="Center"
                WidthRequest="32">
                <Image.Source>
                    <FontImageSource FontFamily="{StaticResource icomoon}" Glyph="{Binding Icon}" />
                </Image.Source>
            </Image>

            <Label
                Grid.Row="0"
                Grid.Column="1"
                FontFamily="{DynamicResource SofiaProMedium}"
                FontSize="15"
                Text="{Binding Text}"
                TextColor="{StaticResource White}"
                VerticalOptions="Center" />
        </Grid>
    </ContentView.Content>
</ContentView>
