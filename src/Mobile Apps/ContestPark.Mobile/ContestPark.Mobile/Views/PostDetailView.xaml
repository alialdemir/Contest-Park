<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ContestPark.Mobile.Views.PostDetailView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:ContestPark.Mobile.Components"
    xmlns:converter="clr-namespace:ContestPark.Mobile.Converters"
    xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    x:Name="postDetailPage"
    prism:ViewModelLocator.AutowireViewModel="True">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:DateTimeMomentConverter x:Key="moment" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <components:NonPersistentSelectedItemListView
                    x:Name="lstMessages"
                    IsRefreshing="False"
                    ItemAppearing="OnItemAppearing"
                    Style="{StaticResource ListView}">
                    <components:NonPersistentSelectedItemListView.Header>
                        <components:PostCardFactoryView BindingContext="{Binding Path=BindingContext.PostModel.Post, Source={x:Reference postDetailPage}}" NavigationService="{Binding Path=BindingContext.NavigationService, Source={x:Reference postDetailPage}}" />
                    </components:NonPersistentSelectedItemListView.Header>
                    <components:NonPersistentSelectedItemListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <components:ListItemView
                                    Date="{Binding Date, Converter={StaticResource moment}}"
                                    Detail="{Binding Comment}"
                                    GotoProfilePageCommand="{Binding Path=BindingContext.GotoProfilePageCommand, Source={x:Reference postDetailPage}}"
                                    Source="{Binding ProfilePicturePath}"
                                    Text="{Binding FullName}"
                                    UserName="{Binding UserName}" />
                            </ViewCell>
                        </DataTemplate>
                    </components:NonPersistentSelectedItemListView.ItemTemplate>
                    <components:NonPersistentSelectedItemListView.Footer>
                        <StackLayout HeightRequest="0" />
                    </components:NonPersistentSelectedItemListView.Footer>
                </components:NonPersistentSelectedItemListView>
            </StackLayout>
            <Grid
                BackgroundColor="#fff"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="End">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="60" />
                </Grid.ColumnDefinitions>

                <Editor
                    x:Name="txtChatbox"
                    Grid.Row="0"
                    Grid.Column="0"
                    AutoSize="TextChanges"
                    AutomationId="txtChatbox"
                    Completed="txtChatbox_Completed"
                    Focused="TxtChatbox_Focused"
                    HorizontalOptions="FillAndExpand"
                    Keyboard="Chat"
                    MaxLength="140"
                    Text="{Binding Message}"
                    TextColor="#7b7b7b"
                    VerticalOptions="FillAndExpand" />

                <ffimageloadingsvg:SvgCachedImage
                    x:Name="btnBtnSendMessage"
                    Grid.Row="0"
                    Grid.Column="1"
                    HeightRequest="40"
                    HorizontalOptions="End"
                    Opacity="0.20"
                    Source="send_message.svg"
                    VerticalOptions="Center"
                    WidthRequest="40">
                    <ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SendCommentCommand}" />
                    </ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                </ffimageloadingsvg:SvgCachedImage>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
