<?xml version="1.0" encoding="UTF-8" ?>
<ContentView x:Class="ContestPark.Mobile.Components.Search.PlayerSearchView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Components="clr-namespace:ContestPark.Mobile.Components"
             xmlns:converters="clr-namespace:ContestPark.Mobile.Converters.Follow"
             xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
             x:Name="playerSearchPage">
    <ContentView.Resources>
        <ResourceDictionary>
            <converters:FollowIconConverter x:Key="followIconConverter" />
            <converters:FollowIconColorConverter x:Key="followIconColorConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    <ContentView.Content>
        <Components:CustomGrid x:Name="cstmGrid"
                               Padding="10,10,0,10"
                               BackgroundColor="{StaticResource White}"
                               CommandParameter="{Binding UserName}"
                               RowSpacing="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="*" />
                <RowDefinition Height="1" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Components:CircleImage x:Name="imgProfilePicture"
                                    Grid.RowSpan="2"
                                    Grid.Column="0"
                                    CommandParameter="{Binding UserName}"
                                    Source="{Binding PicturePath}" />

            <Label Grid.Row="0"
                   Grid.Column="1"
                   FontAttributes="Bold"
                   FontSize="Medium"
                   LineBreakMode="TailTruncation"
                   Text="{Binding FullName}"
                   TextColor="Black" />

            <Label Grid.Row="1"
                   Grid.Column="1"
                   Grid.ColumnSpan="2"
                   FontSize="Default"
                   LineBreakMode="WordWrap"
                   Text="{Binding UserName, StringFormat='@{0}'}"
                   TextColor="#666" />

            <iconize:IconImage Grid.RowSpan="1"
                               Grid.Column="2"
                               Icon="{Binding IsFollow, Converter={StaticResource followIconConverter}}"
                               IconColor="{Binding IsFollow, Converter={StaticResource followIconColorConverter}}"
                               IconSize="22">
                <iconize:IconImage.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding Path=FollowOrUnFollowCommand, Source={x:Reference playerSearchPage}}" />
                </iconize:IconImage.GestureRecognizers>
            </iconize:IconImage>

            <Components:Line Grid.Row="2" Grid.ColumnSpan="3" />
        </Components:CustomGrid>
    </ContentView.Content>
</ContentView>