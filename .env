	# Compose supports declaring default environment variables in an environment file named .env placed in the folder docker-compose command is executed from (current working directory).
# Compose expects each line in an env file to be in VAR=VAL format. Lines beginning with # (i.e. comments) are ignored, as are blank lines.
# Note: Values present in the environment at runtime will always override those defined inside the .env file. Similarly, values passed via command-line arguments take precedence as well.

# The IP below should be swapped to your real IP or DNS name, like 192.168.88.248, etc. if testing from remote browsers or mobile devices

CONTESTPARK_EXTERNAL_DNS_NAME_OR_IP=localhost
CONTESTPARK_PROD_EXTERNAL_DNS_NAME_OR_IP=169.254.80.80

#CONTESTPARK_AZURE_SERVICE_BUS=<YourAzureServiceBusInfo>
#CONTESTPARK_SERVICE_BUS_USERNAME=<ServiceBusUserName-OnlyUsedIfUsingRabbitMQUnderwindows>
#CONTESTPARK_SERVICE_BUS_PASSWORD=<ServiceBusUserPassword-OnlyUsedIfUsingRabbitMQUnderwindows>
CONTESTPARK_AZURE_STORE_ACCOUNT_NAME=<CONTESTPARK_AZURE_STORE_ACCOUNT_NAME>
CONTESTPARK_AZURE_STORE_ACCOUNT_KEY=<CONTESTPARK_AZURE_STORE_ACCOUNT_KEY>
CONTESTPARK_COSMOSDB_SERVICE_ENDPOINT=<CONTESTPARK_COSMOSDB_SERVICE_ENDPOIN>
CONTESTPARK_COSMOSDB_AUTH_KEY_OR_RESOURCE_TOKEN=<CONTESTPARK_COSMOSDB_AUTH_KEY_OR_RESOURCE_TOKEN>