declare -a envs=("CONTESTPARK_GIFT_MONEY_AMOUNT" "CONTESTPARK_AWS_LOG_GROUP_NAME" "CONTESTPARK_AWS_SECRET_ACCESS_KEY" "CONTESTPARK_AWS_ACCESS_KEY_Id" "CONTESTPARK_CLOUDFRONT_URL" "CONTESTPARK_ELASTIC_SEARCH_URI" "CONTESTPARK_ELASTIC_SEARCH_URI" "CONTESTPARK_AZURE_REDIS_SIGNALR_DB" "CONTESTPARK_MYSQL_CONNECTIONSTRING" "CONTESTPARK_AZURE_SERVICE_BUS" "CONTESTPARK_SERVICE_BUS_USERNAME" "CONTESTPARK_SERVICE_BUS_PASSWORD" "CONTESTPARK_BET_COMISSION")
for env in "${envs[@]}"
do
echo "$env"
sed -i 's/$('"$env"')/'\"""$env"\""'/g' deployment.yaml
done